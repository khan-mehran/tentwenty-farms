// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true, // This enables the new SWC compiler for faster builds
    // Add any other custom configuration here
  };
  
  module.exports = nextConfig;
  